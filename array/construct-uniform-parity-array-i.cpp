class Solution {
public:
    bool uniformArray(vector<int>& nums1) {
        // bool ans = false;
        // for(int i = 0;i<n;i++)
        //     {
        //         if(nums[i] % 2 == 0)
        //         {
        //             ans = true;
        //         }
        //     }

        // if(ans) return ans;

        // for(int i = 0;i<n;i++)
        //     {
        //         if(nums[i] % 2 != 0)
        //         {
        //             ans = true;
        //         }
        //     }
        // if(ans) return ans;

        return true;
    }
};